using System;
class Program
{
  static void Main()
  {
    int x = 0;
    <strong>x += 4;</strong>  // x = x + 4
    Console.WriteLine(x);
    <strong>x -= 2;</strong>  // x = x - 2
    Console.WriteLine(x);
    <strong>x *= 3;</strong>  // x = x * 3
    Console.WriteLine(x);
    <strong>x /= 2;</strong>  // x = x / 2
    Console.WriteLine(x);
  }
}
